# nginx101


# Get ready
```bash
    sudo apt-get update && sudo apt-get install nginx

    nginx -v
    >> nginx version: nginx/1.22.1

```


# Everything will happen here:
```bash
cd /etc/nginx
```
